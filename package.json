{
  "name": "muniz-dev-landing-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs && rimraf dist-ssr",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^24.13.3",
    "oxlint": "^1.75.0",
    "rimraf": "^6.1.3",
    "sharp": "^0.35.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "preact": "^10.29.8",
    "preact-render-to-string": "^6.7.0"
  }
}
